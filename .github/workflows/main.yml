name: Run Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  run-tests:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          # Enable caching for faster subsequent runs
          enable-cache: true
          python-version-file: ".python-version"

      - name: Run tests
        # uv run will automatically install dependencies from your lockfile
        run: uv run pytest
